const formAPT = async (req, res) => {
    
    const {firstName, 
        tarefa, 
        area, 
        analiseBem, 
        analiseHabilidato, 
        analiseSaber, 
        participacaoDDS, 
        participacaoDDSTema, 
        episObrigatorios, 
        riscosAtividade} = req.body;

        try{

        } catch(error) {
            console.log("Erro criando formulário", erro)
        }

}

export {formAPT}